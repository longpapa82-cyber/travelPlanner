# Rate limiting zones (2m = 32k IPs, sufficient for single-server deployment)
limit_req_zone $binary_remote_addr zone=general:2m rate=30r/s;
limit_req_zone $binary_remote_addr zone=api:2m rate=10r/s;

# Redirect HTTP -> HTTPS (DuckDNS + new domain)
server {
    listen 80;
    server_name ${DOMAIN} ${DOMAIN_NEW} www.${DOMAIN_NEW};

    # Let's Encrypt challenge
    location /.well-known/acme-challenge/ {
        root /var/www/certbot;
    }

    location / {
        return 301 https://$host$request_uri;
    }
}

# Old domain (DuckDNS) -> new domain redirect
server {
    listen 443 ssl http2;
    server_name ${DOMAIN};

    ssl_certificate /etc/letsencrypt/live/${DOMAIN}/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/${DOMAIN}/privkey.pem;

    return 301 https://${DOMAIN_NEW}$request_uri;
}

# www -> non-www redirect
server {
    listen 443 ssl http2;
    server_name www.${DOMAIN_NEW};

    ssl_certificate /etc/letsencrypt/live/${DOMAIN}/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/${DOMAIN}/privkey.pem;

    return 301 https://${DOMAIN_NEW}$request_uri;
}

# HTTPS server (primary domain only)
server {
    listen 443 ssl http2;
    server_name ${DOMAIN_NEW};

    # Docker embedded DNS — re-resolves container IPs dynamically.
    # Prevents 502 errors when containers are recreated during deployment.
    resolver 127.0.0.11 valid=10s ipv6=off;

    # SSL certificates (Let's Encrypt)
    ssl_certificate /etc/letsencrypt/live/${DOMAIN}/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/${DOMAIN}/privkey.pem;

    # SSL security settings
    ssl_protocols TLSv1.2 TLSv1.3;
    ssl_ciphers ECDHE-ECDSA-AES128-GCM-SHA256:ECDHE-RSA-AES128-GCM-SHA256:ECDHE-ECDSA-AES256-GCM-SHA384:ECDHE-RSA-AES256-GCM-SHA384;
    ssl_prefer_server_ciphers off;
    ssl_session_timeout 1d;
    ssl_session_cache shared:SSL:2m;
    ssl_session_tickets off;

    # Gzip compression
    gzip on;
    gzip_vary on;
    gzip_proxied any;
    gzip_comp_level 6;
    gzip_min_length 256;
    gzip_types
        text/plain
        text/css
        text/javascript
        application/javascript
        application/json
        application/xml
        image/svg+xml
        application/wasm;

    # HSTS
    add_header Strict-Transport-Security "max-age=63072000; includeSubDomains; preload" always;

    # Security headers
    add_header X-Frame-Options "SAMEORIGIN" always;
    add_header X-Content-Type-Options "nosniff" always;
    add_header Referrer-Policy "strict-origin-when-cross-origin" always;
    add_header Permissions-Policy "camera=(), microphone=(), geolocation=(self)" always;

    # Rate limiting
    limit_req zone=general burst=50 nodelay;

    # Block sensitive file types in uploads
    location ~* /uploads/.*\.(key|pem|env|sh|sql|bak|conf|log)$ {
        deny all;
        return 404;
    }

    # Serve uploaded files from backend (photos are stored in backend container)
    location /uploads/ {
        set $backend_target http://backend:3000;
        proxy_pass $backend_target;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        expires 1y;
        add_header Cache-Control "public, immutable";
        access_log off;
    }

    # Proxy to frontend (which handles static files + API proxy)
    location / {
        set $frontend_target http://frontend:8080;
        proxy_pass $frontend_target;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_cache_bypass $http_upgrade;

        # Timeouts for AI trip generation (30-day trips can take ~3 minutes)
        proxy_read_timeout 300s;
        proxy_send_timeout 300s;
    }

    # Stricter rate limit for API — proxy directly to backend (skip frontend nginx)
    location /api/ {
        client_max_body_size 10m;
        limit_req zone=api burst=20 nodelay;
        set $backend_target http://backend:3000;
        proxy_pass $backend_target;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_read_timeout 300s;
        proxy_send_timeout 300s;
    }
}
