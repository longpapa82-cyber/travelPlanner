/**
 * Ad initialization stub (Web)
 *
 * AdMob is native-only. Web ads (AdSense) are initialized
 * lazily when the AdSense component mounts.
 */
export async function initializeAds(): Promise<void> {
  // No-op on web — AdSense loads on demand
}
